[package]
name = "ion-protocol"
version = "0.1.0"
edition = "2024"
description = "ION RPC wire protocol envelopes — request/response/event frames shared by worker, host and CLI (payloads stay opaque)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
